# Boxes App

Django-приложение для управления боксами (шотами) в AI Asset Manager.

## Модели

### Box
- `project` (FK) - проект, к которому относится бокс
- `name` - название бокса
- `order_index` - порядковый номер для сортировки (default=0)
- `created_at` - дата создания
- `updated_at` - дата последнего обновления

**Сортировка:** По умолчанию боксы сортируются сначала по `order_index`, затем по `created_at`.

## Структура
```
boxes/
├── migrations/          # Миграции БД
├── admin.py            # Регистрация в админке
├── apps.py             # Конфигурация приложения
├── models.py           # Модель Box
├── services.py         # Бизнес-логика
├── tests.py            # 9 тестов
└── README.md           # Документация
```

## Администрирование

Модель зарегистрирована в Django Admin с:
- **list_display**: name, project, order_index, created_at
- **list_filter**: project, created_at
- **search_fields**: name
- **list_editable**: order_index - можно менять порядок прямо из списка!
- Группировкой полей в fieldsets

### Возможности админки
- 🔄 Редактирование порядка боксов прямо из списка (list_editable)
- 🔍 Фильтрация по проекту и дате создания
- 🔎 Поиск по названию
- 📊 Отображение всех ключевых полей

## Использование

```python
from apps.boxes.services import (
    create_box, 
    update_box, 
    reorder_boxes, 
    delete_box,
    get_project_boxes
)

# Создание бокса
box = create_box(project=my_project, name="Сцена 1", order_index=0)

# Обновление
box = update_box(box, name="Новое название", order_index=5)

# Изменение порядка боксов
reorder_boxes([box3.id, box1.id, box2.id])  # Новый порядок

# Получение всех боксов проекта (в правильном порядке)
boxes = get_project_boxes(my_project)

# Удаление
delete_box(box)
```

## Связи

- **Project → Box**: один ко многим (related_name='boxes')
- Проект может иметь несколько боксов
- При удалении проекта все его боксы удаляются (CASCADE)

## Тесты

9 unit-тестов покрывают:
- Создание и атрибуты модели
- Строковое представление
- Сортировку по order_index
- Обратную связь через related_name
- Все CRUD операции через сервисы
- Функцию переупорядочивания (reorder)
