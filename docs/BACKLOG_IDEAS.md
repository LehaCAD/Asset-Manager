# Бэклог идей — 2026-03-23

## Frontend / UX

### Навигация: соседние группы
Внутри группы добавить стрелки влево-вправо в навбаре для перехода между соседними группами без возврата на уровень выше. Погружение в подгруппу — через клик на иконку группы. Три уровня: навбар (цена, тема, аккаунт) → хлебные крошки + навигация → вид, фильтры. Решения должны быть продуманными.

### Aspect Ratio: группировка разрешений
15 разрешений — много. Сгруппировать: горизонтальные, вертикальные, квадратные в отдельные категории. Определить на каком уровне это решается (backend schema / frontend UI / оба).

### Меню генерации → бар
Меню генерации сворачивается не полностью, а в компактный бар. В баре доступны быстрые опции.

### Проект-карточки
Сделать красивее. Убрать лишнее с 9:16/16:9 — пусть будет квадрат. Проекты должны отличаться визуально — за основу брать элементы внутри проекта.

### Info panel: использованные изображения
В панели информации элемента показывать маленькие иконки изображений, использованных при генерации. Edge cases: фото удалено, 14+ фото (скрыть с раскрытием).

### Bulk actions
Посмотреть реализацию в Leonardo.ai — понравилась фича "Organize". Адаптировать.

### Отрисовать весь фронт в pen
Отрисовать текущее состояние всех экранов + модалки + попапы. Изменения показывать на этом.

## Backend / Pricing

### Ценообразование и заряды
Проблема: KIE кредиты → доллары → рубли = уродливые цены (7.02 и т.д.). Нужна нормальная конвертация. Внутренняя валюта "заряды" (название не финальное). Максимум дробления 0.5. Посмотреть лучшие практики виртуальных валют.

### Пересчёт цены
Цена должна пересчитываться и ре-рендериться ТОЛЬКО при переключении значимых параметров (модель, разрешение, длительность). Если не хватает денег — стоимость всё равно показывать.

## Пайплайн

### Frontend pipeline
Сначала отрисовать в pen → согласовать → перенос в код → ревью. Связать с UX-анализом. Добавление фичи = комплексная работа под ключ.
