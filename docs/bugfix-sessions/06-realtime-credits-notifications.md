# Сессия 06 — Realtime: баланс кадров и уведомления

**Цель:** баланс кадров и список уведомлений обновляются в реальном времени через WebSocket, без перезагрузки страницы.

---

## BF-06-01 — Баланс кадров не обновляется при начислении админом

**Симптом:**
> «Нету вебсокета. Вот я из чата поддержки начисляю кадры, вознаграждаю за отзыв — а количество кадров, отображаемое у пользователя, не меняется. После обновления страницы только обновляется».

**Где:**
- WS consumer для `user_{id}` group (см. `apps/subscriptions/signals.py` — там есть `group_send` для `subscription_changed`, нужно добавить аналог для `credits_changed`).
- `frontend/lib/store/credits.ts` — подписка на WS.

**Действия:**
1. На бэкенде — отправлять `group_send('user_{id}', {'type': 'credits_changed', 'balance': новый})` при любом изменении `CreditsTransaction` (post_save на модели).
2. На фронте — consumer в WS-клиенте, обновляет store.
3. Убедиться, что нет гонки: баланс приходит и через HTTP, и через WS — использовать «последний по времени» или сравнивать.

**Тест:** админ начисляет → за ≤2 сек баланс обновляется у юзера без F5.

---

## BF-06-02 — Ачивка не создаёт нотификацию

**Дубль [BF-05-03](05-onboarding-achievements.md).** В этой сессии — часть про WS-доставку: когда ачивка создаёт Notification, оно должно прилететь по WS в список уведомлений.

---

## BF-06-03 — Инвентаризация WS-событий

**Задача:** собрать все WS-события, которые бэкенд умеет слать, и сверить с тем, что фронт слушает.

- `element_status_changed` — ✓
- `subscription_changed` — ?
- `notification_created` — ?
- `credits_changed` — добавить
- `achievement_earned` — добавить

**Результат:** таблица в этом файле + обновить `docs/systems/infra.md` секцию про WS-события.
