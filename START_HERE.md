# ⚡ НАЧНИТЕ ОТСЮДА - Деплой за 3 минуты

## 🎯 Что у вас есть

✅ Сервер: **85.239.56.80**  
✅ Домен: **raskadrawka.ru**  
✅ SSH доступ: `root@85.239.56.80` (пароль: `qk_sAs#7qBPE5j`)  
✅ Все конфигурации готовы!

---

## 🚀 Деплой - пошаговая инструкция

### 1️⃣ Откройте файл `БЫСТРЫЙ_ГАЙД.txt`

Там краткая шпаргалка со всеми командами.

### 2️⃣ Подключитесь к серверу

```bash
ssh root@85.239.56.80
# Пароль: qk_sAs#7qBPE5j
```

### 3️⃣ Следуйте командам из файла

Всё расписано по порядку:
1. ✅ Установка Docker
2. ✅ Загрузка проекта (через WinSCP или архив)
3. ✅ Создание .env.production
4. ✅ Запуск контейнеров
5. ✅ Миграции и создание админа

**Время:** ~15-20 минут

---

## 🎉 После деплоя

Откройте в браузере:

- 🌐 **Сайт**: http://raskadrawka.ru
- 🔐 **Админка**: http://raskadrawka.ru/admin
- 📡 **API**: http://raskadrawka.ru/api

---

## 🔄 Обновление проекта

Когда внесли изменения в код:

1. Загрузите новые файлы через WinSCP в `/var/www/asset-manager`
2. На сервере выполните:

```bash
cd /var/www/asset-manager
docker compose -f docker-compose.production.yml down
docker compose -f docker-compose.production.yml up -d --build
docker compose -f docker-compose.production.yml exec backend python manage.py migrate
```

**За 2-3 минуты** изменения применятся!

---

## 🔒 Настройка HTTPS (опционально, но рекомендуется)

После первого деплоя выполните на сервере:

```bash
# 1. Подключитесь к серверу
ssh root@85.239.56.80

# 2. Перейдите в папку проекта
cd /var/www/asset-manager

# 3. Получите SSL сертификат (замените email на свой)
docker compose -f docker-compose.production.yml run --rm certbot certonly \
  --webroot \
  --webroot-path=/var/www/certbot \
  -d raskadrawka.ru \
  -d www.raskadrawka.ru \
  --email your-email@example.com \
  --agree-tos \
  --no-eff-email

# 4. Включите HTTPS в nginx
nano nginx/conf.d/default.conf
# Найдите строку "# return 301 https://" и удалите "#"
# Найдите блок "# server {" с портом 443 и раскомментируйте весь блок

# 5. Перезапустите nginx
docker compose -f docker-compose.production.yml restart nginx

# 6. Готово! Теперь сайт работает по HTTPS
```

---

## 📊 Полезные команды

### Посмотреть логи
```bash
ssh root@85.239.56.80
cd /var/www/asset-manager
docker compose -f docker-compose.production.yml logs -f
```

### Перезапустить сервисы
```bash
docker compose -f docker-compose.production.yml restart
```

### Остановить/Запустить
```bash
docker compose -f docker-compose.production.yml down
docker compose -f docker-compose.production.yml up -d
```

---

## ❓ Что-то пошло не так?

1. **Проверьте логи** (команда выше)
2. **Проверьте статус контейнеров**:
   ```bash
   ssh root@85.239.56.80
   docker ps
   ```
3. **Перезапустите всё**:
   ```bash
   cd /var/www/asset-manager
   docker compose -f docker-compose.production.yml restart
   ```

---

## 📚 Дополнительная документация

- **[QUICK_START.md](QUICK_START.md)** - Быстрый старт
- **[DEPLOY.md](DEPLOY.md)** - Подробная инструкция
- **[README.md](README.md)** - Общая информация о проекте

---

## 🎨 Workflow разработки

```
Работаете локально → Вносите изменения → .\quick-deploy.ps1 → Готово!
```

Это всё! Приятной работы! 🚀
