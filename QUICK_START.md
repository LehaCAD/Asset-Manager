# ⚡ Быстрый старт - Деплой на VPS за 15 минут

## 🎯 Что нужно сделать

### 1️⃣ Откройте файл `БЫСТРЫЙ_ГАЙД.txt` или `РУЧНОЙ_ДЕПЛОЙ.md`

Там пошаговая инструкция со всеми командами.

### 2️⃣ Подключитесь к серверу

```bash
ssh root@85.239.56.80
# (пароль: qk_sAs#7qBPE5j)
```

### 3️⃣ Следуйте инструкциям

Вкратце:
1. Установите Docker на сервере
2. Загрузите проект (через WinSCP или архив)
3. Создайте .env.production
4. Запустите Docker контейнеры
5. Примените миграции

**Готово!** Сайт будет доступен по адресу: http://raskadrawka.ru

### 2️⃣ Создайте суперпользователя (только первый раз)

После первого деплоя подключитесь к серверу:

```bash
ssh root@85.239.56.80
cd /var/www/asset-manager
docker compose -f docker-compose.production.yml exec backend python manage.py createsuperuser
```

Введите логин, email и пароль для входа в админку.

### 3️⃣ Настройте HTTPS (необязательно, но рекомендуется)

На сервере выполните:

```bash
cd /var/www/asset-manager

# Получите SSL сертификат
docker compose -f docker-compose.production.yml run --rm certbot certonly \
  --webroot \
  --webroot-path=/var/www/certbot \
  -d raskadrawka.ru \
  -d www.raskadrawka.ru \
  --email your-email@example.com \
  --agree-tos \
  --no-eff-email

# Отредактируйте nginx конфиг
nano nginx/conf.d/default.conf
# Раскомментируйте HTTPS секцию (удалите # в начале строк)

# Перезапустите nginx
docker compose -f docker-compose.production.yml restart nginx
```

---

## 🔄 Обновление проекта

Когда внесли изменения в код:

1. Загрузите обновленные файлы через WinSCP в `/var/www/asset-manager`

2. На сервере выполните:

```bash
cd /var/www/asset-manager
docker compose -f docker-compose.production.yml down
docker compose -f docker-compose.production.yml up -d --build
docker compose -f docker-compose.production.yml exec backend python manage.py migrate
```

**Всё!** Изменения применятся за 2-3 минуты.

---

## 🛠️ Полезные команды

### Проверка логов

```bash
# Подключитесь к серверу
ssh root@85.239.56.80

# Перейдите в папку проекта
cd /var/www/asset-manager

# Смотрите логи
docker compose -f docker-compose.production.yml logs -f

# Логи конкретного сервиса
docker compose -f docker-compose.production.yml logs -f backend
docker compose -f docker-compose.production.yml logs -f frontend
```

### Перезапуск сервисов

```bash
# Перезапустить всё
docker compose -f docker-compose.production.yml restart

# Или конкретный сервис
docker compose -f docker-compose.production.yml restart backend
```

### Остановка/Запуск

```bash
# Остановить
docker compose -f docker-compose.production.yml down

# Запустить
docker compose -f docker-compose.production.yml up -d
```

---

## 📍 Важные адреса

- 🌐 **Сайт**: http://raskadrawka.ru (или https:// после настройки SSL)
- 🔐 **Админка**: http://raskadrawka.ru/admin
- 📡 **API**: http://raskadrawka.ru/api

---

## ❓ Что-то пошло не так?

1. **Проверьте логи** (команда выше)
2. **Перезапустите сервисы** (команда выше)
3. **Проверьте что Docker запущен**:
   ```bash
   docker ps
   ```
4. Смотрите подробную инструкцию в файле `DEPLOY.md`

---

## 🎨 Workflow разработки

```
1. Работаете локально на Windows
   └─> Редактируете код в Cursor/VSCode
   └─> Тестируете: npm run dev, python manage.py runserver

2. Сохраняете изменения
   └─> git add . && git commit -m "..."

3. Деплоите на сервер
   └─> .\quick-deploy.ps1

4. Готово! ✨
```

---

Это всё! Теперь вы можете работать с кодом локально и за 1-2 минуты деплоить изменения на сервер одной командой.
